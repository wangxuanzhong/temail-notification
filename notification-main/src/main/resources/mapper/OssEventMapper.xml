<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.syswin.temail.notification.main.infrastructure.OssEventMapper">
  <delete id="deleteTemail">
    delete from `event` where `to` = #{to};
    delete from `topic_event` where `to` = #{to};
    delete from `unread` where `to` = #{to};
    delete from `member` where `temail` = #{to}
  </delete>
</mapper>